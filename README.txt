READ ME

To run this file:
Host Computer must have mongodb & node.js installed

Use git(Windows) Termianl (OSX)
First begin mongodb with the command 'mongod'
Open a new terminal/git window
To nagivate to this folder
Run 'npm install' within the folder to download all the required dependcies
run the server file by using one of of the following two commands in the terminal 'nodemon' | 'npm start www/bin'
Open a broswer and navigate to the web app by

https://localhost:4002

:) 
 